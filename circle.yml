general:
  artifacts:
    # Compiled assets
    - plottable.js
    - plottable.css
    # Demo paths
    - demo
    - quicktests
    # Modules used by quicktests
    - node_modules/d3
    - node_modules/jquery
    - node_modules/requirejs
machine:
  node:
    version: 6.1
test:
  override:
    - npm test
deployment:
  demo:
    branch: /.*/
    commands:
      - npm start -- demo-compile
      - ./demo.js
