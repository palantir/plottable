general:
  artifacts:
    - demo
    - plottable.css
machine:
  node:
    version: 6.1
test:
  override:
    - npm test
deployment:
  demo:
    branch: /.*/
    commands:
      - npm start -- demo-compile
      - ./demo.js
